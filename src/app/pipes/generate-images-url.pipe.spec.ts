import { GenerateImagesUrlPipe } from './generate-images-url.pipe';

describe('GenerateImagesUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateImagesUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
